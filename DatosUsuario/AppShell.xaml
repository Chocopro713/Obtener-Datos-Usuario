<?xml version="1.0" encoding="UTF-8"?>
<Shell
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="DatosUsuario.AppShell"
    xmlns:home="clr-namespace:DatosUsuario.Views.Home"
    xmlns:login="clr-namespace:DatosUsuario.Views.Login">
    
    <!--  Login  -->
    <ShellContent ContentTemplate="{DataTemplate home:HomePage}" Route="home" />
    <ShellContent ContentTemplate="{DataTemplate home:UsersPage}" Route="users" />
    <ShellContent ContentTemplate="{DataTemplate home:DataUserPage}" Route="datauser" />
    <ShellContent ContentTemplate="{DataTemplate login:LoginPage}" Route="login" />

</Shell>
